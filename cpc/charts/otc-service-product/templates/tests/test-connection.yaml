apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "otc-service-product.fullname" . }}-test-connection"
  labels:
    {{- include "otc-service-product.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "otc-service-product.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
